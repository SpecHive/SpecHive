{
  "name": "assertly",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "description": "Assertly monorepo",
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,yaml,yml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,yaml,yml}\"",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm test:unit && pnpm test:integration",
    "test:unit": "pnpm -r run test",
    "test:integration": "vitest run --config test/vitest.integration.config.ts",
    "build": "pnpm -r run build",
    "db:generate": "pnpm -r --filter='*' run db:generate",
    "db:migrate": "pnpm -r --filter='*' run db:migrate",
    "db:seed": "pnpm -r --filter='*' run db:seed",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "prepare": "husky"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "argon2",
      "esbuild"
    ],
    "overrides": {
      "minimatch@>=10.0.0 <10.2.3": ">=10.2.3"
    }
  },
  "devDependencies": {
    "@assertly/eslint-config": "workspace:*",
    "@assertly/typescript-config": "workspace:*",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0",
    "@commitlint/cli": "^20.0.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@swc/core": "^1.15.17",
    "argon2": "^0.44.0",
    "eslint": "^9.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.0.0",
    "postgres": "^3.4.5",
    "prettier": "^3.4.2",
    "typescript": "^5.9.3",
    "unplugin-swc": "^1.5.9",
    "vitest": "^4.0.0"
  }
}
